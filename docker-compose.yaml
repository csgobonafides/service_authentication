version: '3.4'

services:
  redis:
    image: 'redis:alpine'
    container_name: redis
    restart: always
    volumes:
      - redis-data:/data
    ports:
      - '6379:6379'

  app:
    container_name: app
    build: ./
    ports:
      - '8000:8000'
    restart: always

volumes:
  redis-data: